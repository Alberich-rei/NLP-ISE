
# ISE — 课程特化版（Windows / PyCharm 可运行）

本项目为课程作业的“课程特化版（选项 C）”，目的是以最清晰、可交付且能在 Windows + PyCharm 上直接运行的形式，
满足作业评估要点（智能源选择 / 本地 RAG / 高级重排序 / 工作流 / 多模态 / 领域工具）。

## 一、环境准备
pip install -r requirements.txt

## 二、构建 RAG（第一次运行前）
python rag_builder.py

## 三、启动系统
python main.py
